name: todo_f
description: "A new Flutter project."

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=3.3.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  ferry: ^0.16.0+1
  ferry_flutter: ^0.9.0-dev.3
  gql_http_link: ^1.0.1+1
  hive: ^2.2.3
  get_it: ^7.7.0
  ferry_hive_store: ^0.5.2
  hive_flutter: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.9
  json_serializable: ^6.7.0
  ferry_generator: ^0.10.0
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

